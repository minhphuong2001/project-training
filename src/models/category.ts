export interface ICategoryData {
    id: string | number;
    parentId: string | number;
    name: string;
    path: string;
    pos: string | number;
}

export interface IShippingData {
    id: string | number;
    name: string;
}